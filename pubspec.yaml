name: flutter_number_trivia_standalone
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  equatable: ^1.0.2
  dartz: ^0.8.9
  shared_preferences: ^0.5.6
  http: ^0.12.0+4
  data_connection_checker: ^0.3.4
  flutter_bloc: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
